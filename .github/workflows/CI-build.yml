name: CI-build

on:
  push:
    branches: [main]
    paths-ignore:
      - '**.md'
      - LICENSE
  pull_request:
    branches: [main]
    paths-ignore:
      - '**.md'
      - LICENSE
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-build-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  windows:
    strategy:
      fail-fast: false
      matrix:
        arch: [x64, arm64]
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - uses: pnpm/action-setup@v4

      - name: Install uv
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          version: 0.11.28
          python-version: '3.11'
          enable-cache: true
          cache-dependency-glob: core/uv.lock

      - name: Install dependencies
        run: |
          pnpm install --frozen-lockfile
          uv sync --project core --locked --no-default-groups --group dev

      - name: Build bundled core
        run: pnpm run core:bundle

      - name: Copy bundled core
        run: Copy-Item core\dist\Final2x-core resources\Final2x-core -Recurse

      - name: Smoke-test bundled core
        run: resources\Final2x-core\Final2x-core.exe -h

      - name: Build desktop app
        run: pnpm run build:win-${{ matrix.arch }}
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Archive unpacked app
        shell: pwsh
        run: |
          $directory = if ('${{ matrix.arch }}' -eq 'x64') { 'dist/win-unpacked' } else { 'dist/win-arm64-unpacked' }
          7z a -mx=1 -r "Final2x-windows-${{ matrix.arch }}-unpacked.7z" "$directory/*"

      - name: Upload unpacked app
        uses: actions/upload-artifact@v4
        with:
          name: Final2x-windows-${{ matrix.arch }}-unpacked
          path: Final2x-windows-${{ matrix.arch }}-unpacked.7z

  macos:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - uses: pnpm/action-setup@v4

      - name: Install uv
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          version: 0.11.28
          python-version: '3.11'
          enable-cache: true
          cache-dependency-glob: core/uv.lock

      - name: Install dependencies
        run: |
          pnpm install --frozen-lockfile
          uv sync --project core --locked --no-default-groups --group dev

      - name: Build bundled core
        run: pnpm run core:bundle

      - name: Copy bundled core
        run: cp -R core/dist/Final2x-core resources/

      - name: Smoke-test bundled core
        run: resources/Final2x-core/Final2x-core -h

      - name: Build desktop app
        run: pnpm run build:mac-arm64
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Smoke-test packaged core
        run: dist/mac-arm64/Final2x.app/Contents/Resources/Final2x-core/Final2x-core -h

      - name: Archive unpacked app
        run: 7z a -mx=1 -r Final2x-macos-arm64-unpacked.7z dist/mac-arm64/*

      - name: Upload dmg
        uses: actions/upload-artifact@v4
        with:
          name: Final2x-macos-arm64-dmg
          path: dist/*.dmg

      - name: Upload unpacked app
        uses: actions/upload-artifact@v4
        with:
          name: Final2x-macos-arm64-unpacked
          path: Final2x-macos-arm64-unpacked.7z

  linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - uses: pnpm/action-setup@v4

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build desktop app
        run: pnpm run build:linux-x64
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Archive unpacked app
        run: 7z a -mx=1 -r Final2x-linux-pip-x64-unpacked.7z dist/linux-unpacked/*

      - name: Upload packages
        uses: actions/upload-artifact@v4
        with:
          name: Final2x-linux-pip-x64
          path: |
            dist/*.snap
            dist/*.AppImage
            dist/*.deb
            Final2x-linux-pip-x64-unpacked.7z
